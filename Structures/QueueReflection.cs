namespace Structures;

public class QueueReflection<T>
{
    // Сравнивая мое решение с эталонным, я вижу, что основная структура у нас одинаковая.
    // В моем решении назвал метод Peek() вместо get(), что вроде более стандартно для 
    // операции просмотра без удаления. Я добавил пару методов, которых нет в эталоне - Clear() 
    // и IsEmpty().
    // Статусы назвал GetPeekStatus() вместо get_get_status(), что выглядит чуть понятнее, хотя это мелочь.
    // В целом решения очень похожи по сути. Мое получилось чуть более развернутым, а эталонное решение
    // более минималистичным.
}